<main id="details">
    <section id="details-info">
        <div class="game-image">
            <img src="{{game.image}}">
        </div>

        <div class="game-info">
            <div class="game-text">
                <h1 id="name">{{game.name}}</h1>
                <h3 id="platform">Platform: {{game.platform}}</h3>
                <p id="price"><span>Price: ${{game.price}}</span></p>
                <p id="genre"><span>Genre: {{game.genre}}</span></p>
                <p id="description">{{game.description}}</p>
            </div>
            {{#unless username}}
            <!-- If there is no registered user, do not display buttons -->
            {{else}}
            <div class="product-btn">
                {{#if game.isOwner}}
                <!--Only for registered user and author of the publication -->
                <div class="author">
                    <a href="/game/{{game._id}}/edit" class="btn-edit">Edit</a>
                    <a href="/game/{{game._id}}/delete" class="btn-delete">Delete</a>
                </div>
                {{else}}
                    {{#unless game.enrolled}}
                <!-- Logged in user who has not yet buy this game code-->
                    <a href="/game/{{game._id}}/enroll" class="btn-buy">Buy</a>
                    {{else}}
                <!-- Logged in user who has already buy this game code-->
                    <p class="buy-message">You have already bought this game</p>
                    {{/unless}}
                {{/if}}
            </div>
            {{/unless}}
        </div>
    </section>
</main>